{
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:e2e": "concurrently \"npm run dev:e2e --prefix server\" \"npm run dev --prefix client\"",
    "reset:e2e": "npm run reset:e2e --prefix server"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.2.0",
    "concurrently": "^9.2.4",
    "cross-env": "^10.1.0"
  }
}
